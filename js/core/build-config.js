/* RentBill Pro — build-time Supabase config injection.
 *
 * This file is committed as a SAFE stub (no credentials). During a Cloudflare
 * Pages build, scripts/generate-build-config.js overwrites this file with the
 * real anon key + project URL taken from Pages environment variables, so real
 * credentials never appear in the GitHub repo.
 *
 * Injected globals are read by js/core/config.js (window.__RENTBILL_SB_URL__ /
 * window.__RENTBILL_SB_KEY__) before the app initializes Supabase.
 */
window.__RENTBILL_SB_URL__ = '';
window.__RENTBILL_SB_KEY__ = '';
